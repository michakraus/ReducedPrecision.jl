var documenterSearchIndex = {"docs":
[{"category":"section","location":"lotka_volterra_4d/#Lotka–Volterra-4D","page":"Lotka–Volterra 4D","text":"The 4D Lotka–Volterra system is, like the 2D one, a degenerate Lagrangian system. It is built from the LotkaVolterra4dLagrangian module (an EulerLagrange-generated Lagrangian of the form L = tfrac12 (log q)^T A dot q  q + q^T B dot q - H(q)) as a LODE, using the quasi-canonical reduced gauge (A = A_quasicanonical_reduced, together with the exact one-form B) so that the discrete degenerate system is non-singular.\n\nAs for the 2D case, only variational / implicit-midpoint methods apply. The comparison here uses three of them:\n\nImplicit Midpoint,\nVPRK Gauss(1),\nPMVI Midpoint.\n\nCMDVI is omitted for the 4D system: on this 4D degenerate Lagrangian its Newton/DogLeg iterate leaves the positive orthant and the nonlinear solve breaks down (singular Jacobian for the reduced gauge, divergence for the others), so it cannot complete the integration. The other three methods integrate it without trouble.\n\nThere is no closed-form solution; the solution error is measured against a Float64 Gauss(8) reference. All three methods are type-pure at every precision.","title":"Lotka–Volterra 4D"},{"category":"section","location":"lotka_volterra_4d/#Short-scenario-(Δt-0.01,-t-10)","page":"Lotka–Volterra 4D","text":"","title":"Short scenario (Δt = 0.01, t ≤ 10)"},{"category":"section","location":"lotka_volterra_4d/#Energy-error","page":"Lotka–Volterra 4D","text":"(Image: Energy error, variational integrators)\n\nAt Float32 and Float64 the three variational integrators conserve energy well; at Float16 the VPRK and PMVI solves fail with a NaN in the nonlinear-solver direction — the iterate leaves the positive orthant where the degenerate Lagrangian's log(q) is defined — while implicit midpoint still runs at the short step. BFloat16 behaves much the same — see the 2D discussion for why the degenerate-Lagrangian systems are the one family whose half-precision failures are genuine rather than bookkeeping artefacts.","title":"Energy error"},{"category":"section","location":"lotka_volterra_4d/#Solution-error","page":"Lotka–Volterra 4D","text":"(Image: Solution error, variational integrators)\n\nAgainst the Gauss(8) reference the three integrators keep a small trajectory error at Float32/Float64; at Float16 only implicit midpoint survives to contribute.","title":"Solution error"},{"category":"section","location":"lotka_volterra_4d/#Configuration-space-trajectory","page":"Lotka–Volterra 4D","text":"(Image: Trajectory, variational integrators)","title":"Configuration-space trajectory"},{"category":"section","location":"lotka_volterra_4d/#Coarse-scenario-(Δt-0.1,-t-100)","page":"Lotka–Volterra 4D","text":"","title":"Coarse scenario (Δt = 0.1, t ≤ 100)"},{"category":"section","location":"lotka_volterra_4d/#Energy-error-2","page":"Lotka–Volterra 4D","text":"(Image: Energy error, variational integrators)\n\nAt the coarser step and longer horizon the differences between the variational integrators, and the effect of precision on their long-time energy behaviour, are accentuated.","title":"Energy error"},{"category":"section","location":"lotka_volterra_4d/#Solution-error-2","page":"Lotka–Volterra 4D","text":"(Image: Solution error, variational integrators)\n\nOver the longer horizon the trajectory error against the reference likewise separates the three integrators at the coarse step.","title":"Solution error"},{"category":"section","location":"lotka_volterra_4d/#Configuration-space-trajectory-2","page":"Lotka–Volterra 4D","text":"(Image: Trajectory, variational integrators)","title":"Configuration-space trajectory"},{"category":"section","location":"methodology/#Methodology","page":"Methodology","text":"","title":"Methodology"},{"category":"section","location":"methodology/#Precisions","page":"Methodology","text":"Every experiment is run at four precisions:\n\nconst PRECISIONS = (BFloat16, Float16, Float32, Float64)\n\nThe order is ascending significand precision, which is also the left-to-right panel order in every figure. The two 16-bit formats differ in how they spend those bits:\n\nType Significand bits eps Max finite\nBFloat16 (BFloat16s.jl) 8 2⁻⁷ ≈ 7.8e-3 ≈ 3.4e38\nFloat16 11 2⁻¹⁰ ≈ 9.8e-4 65504\nFloat32 24 ≈ 1.2e-7 ≈ 3.4e38\nFloat64 53 ≈ 2.2e-16 ≈ 1.8e308\n\nBFloat16 keeps Float32's exponent range at the cost of three significand bits, so it is the coarsest of the four for accuracy while being the hardest to overflow. That trade-off is what makes it interesting here: the exponent range buys nothing for these bounded Hamiltonian problems, while the lost significand bits cost accuracy directly.\n\nBFloat16s.jl does not yet define every Base method the stack needs (rem — and therefore fld/div/mod, on which float-range construction depends — plus Integer(::BFloat16) and construction from BigInt). src/bfloat16_compat.jl fills those in following BFloat16s' own Float32 round-trip pattern.\n\nNaNMath has the same gap, and it is the larger one. Its domain-guarded functions — the variants that return NaN where Base would throw a DomainError, which is what lets an implicit solver reject a trial state outside a model's domain instead of aborting the run — are defined only for Float16/Float32/Float64. Since GeometricProblems v0.8.0 passes nanmath = true to every symbolically generated problem, an EulerLagrange-generated vector field reaches the NaNMath variant of every elementary function it contains, so all eleven (sin, cos, tan, asin, acos, acosh, atanh, log, log2, log10, log1p) are shimmed, mirroring NaNMath's own guards.","title":"Precisions"},{"category":"section","location":"methodology/#Methods","page":"Methodology","text":"The methods are split into a geometric (symplectic) group and a non-geometric group. The line style in every plot follows this classification — solid for geometric, dashed for non-geometric.\n\nMethod Group Order Notes\nSymplectic Euler A geometric 1 symplectic partitioned Euler\nSymplectic Euler B geometric 1 symplectic partitioned Euler\nImplicit Midpoint geometric 2 Gauss(1) — symplectic, symmetric\nImplicit Runge-Kutta 4 geometric 4 Gauss(2) — symplectic, symmetric\nExplicit Euler non-geometric 1 energy-increasing\nImplicit Euler non-geometric 1 energy-dissipating\nExplicit Midpoint non-geometric 2 \nExplicit Runge-Kutta 4 non-geometric 4 classical RK4\n\nThe last four form the other methods plotting group as a 2 × 2: explicit versus implicit at order 2, then at order 4. Within each order the only difference is explicit versus implicit; within each of those, only the order. The implicit pair are the Gauss collocation rules — Gauss(1) is the implicit midpoint rule and keeps that label, while Gauss(2) is labelled Implicit Runge-Kutta 4 to pair it with the classical explicit fourth-order rule.\n\nTwo consequences worth knowing. Using Gauss(s) rather than ImplicitMidpoint() puts both implicit rules on the Runge–Kutta code path, which is what lets them share the tableau-driven initial guess described under Initial guess. And on a partitioned problem Gauss(2) reduces to the duplicated partitioned Gauss(2) tableau, so Implicit Runge-Kutta 4 and PRK Gauss(2) below are the same integrator — verified bit-identical at every precision. It appears in both groups deliberately: as the fourth-order implicit member of the 2 × 2, and as the baseline of the implementation-variant comparison.","title":"Methods"},{"category":"section","location":"methodology/#Partitioned-Gauss(2)-variants","page":"Methodology","text":"A third comparison group holds four flavours of the 2-stage Gauss (partitioned midpoint) rule that are algebraically the same method but differ in implementation detail:\n\nMethod Construction\nPRK Gauss(2) PartitionedTableau(Gauss(2)) — the Gauss tableau duplicated for q and p\nSPRK Gauss(2) SymplecticPartitionedTableau(Gauss(2)) — the p-tableau is the symplectic conjugate, so symplecticity holds to floating-point accuracy by construction\nPRK Gauss(2), â=b̂=ĉ=0 as PRK, with the rounding-error compensation coefficients zeroed\nSPRK Gauss(2), â=b̂=ĉ=0 as SPRK, with the rounding-error compensation coefficients zeroed\n\nThese are all symplectic (drawn solid) and let the study isolate how the symplectic-vs-duplicated tableau construction and the compensated-summation coefficients â, b̂, ĉ affect energy conservation in reduced precision.","title":"Partitioned Gauss(2) variants"},{"category":"section","location":"methodology/#Variational-integrators-for-the-degenerate-Lagrangian-problems","page":"Methodology","text":"The Lotka–Volterra problems are degenerate Lagrangian systems (IODE/LODE), on which the explicit, symplectic-Euler and DIRK methods above are undefined. They are compared instead with several flavours of the implicit midpoint rule that do apply: Implicit Midpoint, VPRK(Gauss(1)), PMVImidpoint and (2D only) CMDVI. VPRK(Gauss(1)) is rebuilt at the run precision so it stays type-pure; CMDVI is omitted for the 4D system, where it fails to converge.","title":"Variational integrators for the degenerate-Lagrangian problems"},{"category":"section","location":"methodology/#Nonlinear-solver","page":"Methodology","text":"The implicit methods' stage equations are solved with the trust-region DogLeg solver of SimpleSolvers (rather than a line-search Newton iteration), which is more robust in reduced precision. Explicit methods carry no solver. The solver is a keyword of run_study / integrate_bounded (default DogLeg()), so Newton() can be selected for comparison.","title":"Nonlinear solver"},{"category":"section","location":"methodology/#One-problem-form-for-all-methods","page":"Methodology","text":"The literal method list cannot be run on a single problem form: the special ExplicitEuler / ImplicitEuler of GeometricIntegratorsBase are ODE-only, while SymplecticEulerA / B require a partitioned (PODE/HODE) problem. The resolution is to use the partitioned form for every problem and to represent explicit/implicit Euler by their numerically identical Runge–Kutta tableau twins ExplicitEulerRK / ImplicitEulerRK, which auto-promote to partitioned Runge–Kutta on a PODE/HODE. A single partitioned problem then runs the entire method set, giving a fair geometric-vs-non-geometric comparison.","title":"One problem form for all methods"},{"category":"section","location":"methodology/#Problems","page":"Methodology","text":"Problem Form Precision handling Reference solution\nHarmonic oscillator podeproblem ::Type{T} constructor analytic exact_solution\nPendulum podeproblem ::Type{T} constructor Float64 Gauss(8)\nDouble pendulum hodeproblem hand-built T-typed inputs Float64 Gauss(8)\nToda lattice (N = 16) hodeproblem hand-built T-typed inputs Float64 Gauss(8)\nLotka–Volterra 2D lodeproblem (LotkaVolterra2dSingular) hand-built T-typed inputs Float64 Gauss(8)\nLotka–Volterra 4D lodeproblem (LotkaVolterra4dLagrangian, A_quasicanonical_reduced) hand-built T-typed inputs Float64 Gauss(8)\n\nThe double pendulum, Toda lattice and both Lotka–Volterra problems are generated symbolically by EulerLagrange and have no ::Type{T} constructor, so make_problem(T) builds the initial conditions, timespan, timestep and parameters at precision T explicitly. The Toda lattice additionally carries a lattice size N (here N = 16, kept small so the sweep — in particular the high-order reference and the implicit solves — stays tractable) and a Hamiltonian hamiltonian(t, q, p, params, N) that takes N, so a closure is passed to the energy-error routine.\n\nThe Lotka–Volterra problems are degenerate Lagrangian systems posed as LODEs; the 4D case uses the quasi-canonical reduced gauge matrix A_quasicanonical_reduced (with the exact one-form B) so the discrete system is non-singular.","title":"Problems"},{"category":"section","location":"methodology/#Scenarios","page":"Methodology","text":"Each problem is run in two scenarios, a fine short-horizon run and a coarser one:\n\nShort / fine step — harmonic oscillator and pendulum use Δt = 0.1, t ≤ 1000; the Toda lattice uses Δt = 0.1, t ≤ 100; the double pendulum uses Δt = 0.01, t ≤ 10 (its natural timescale is much shorter); the Lotka–Volterra problems use Δt = 0.01, t ≤ 10.\nCoarse step — harmonic oscillator and pendulum use Δt = 1, t ≤ 10 000; the Toda lattice uses Δt = 1 over the same t ≤ 100 as its short run; the double pendulum uses Δt = 0.1 over the same t ≤ 10 as its short run; the Lotka–Volterra problems use Δt = 0.1, t ≤ 100.\n\nEvery precision runs the same horizon. The output figure filenames encode the timestep (e.g. …_dt_0.1_…), so the two scenarios of a problem are distinguished by Δt rather than by a \"longtime\" label.","title":"Scenarios"},{"category":"section","location":"methodology/#Time-stepping-in-a-local-frame","page":"Methodology","text":"A reduced-precision clock saturates long before a reduced-precision state becomes useless. Once ulp(t) ≥ Δt the stored time stops advancing (t + Δt == t), and the implicit methods' HermiteExtrapolation initial guess — which divides by t₁ - t₀ — fails outright with t₀ == t₁. The onset depends on Δt and is much earlier for BFloat16 than for Float16 (capped_final_time measures it):\n\nΔt BFloat16 Float16 Float32 / Float64\n0.01 t ≈ 2 t ≈ 16 no limit at these horizons\n0.1 t ≈ 16 t ≈ 128 no limit at these horizons\n1.0 t ≈ 256 t ≈ 2048 no limit at these horizons\n\nCapping every horizon to those values would leave almost nothing to study. But the limit is an artefact of bookkeeping, not a property of the integrator: none of the methods here derives its step size from a difference of clock values — they all read Δt from the problem — so the absolute time only ever reaches the vector field, and all six problems are autonomous, with t an unused argument in every vector field and Hamiltonian.\n\nintegrate_bounded therefore advances the solution step in a fixed local time frame (reset_local!): after each history shift the stored times are relabelled so that every step looks like the second one — history at 0, Δt, target at 2Δt. Consecutive times then stay exactly Δt apart at any precision and any horizon, and Hermite's extrapolation parameter is exactly s = 2.\n\nThis is a relabelling, not a change of dynamics. Explicit methods (which carry no initial guess) produce bit-identical results either way; implicit methods differ only at solver tolerance (~1e-16 in Float64, ~1e-7 in Float32). It is also strictly more accurate than a global clock that has drifted without yet saturating: Float16 resolves only about 0.004 near t = 5, comparable to Δt = 0.01, so a differenced interval there is materially wrong. Two details it depends on: the whole history must be relabelled after reset! (which copies stale times along with the state), and the frame must be anchored at non-negative times, because MidpointExtrapolation's sub-stepping loop for partitioned problems tests abs(t + extrap.Δt) < abs(sol.t).\n\nPass localclock = false to run_study to step along the problem's own T-typed grid instead; it is retained only to demonstrate the failure. A non-autonomous problem would need it, together with a horizon capped by capped_final_time.","title":"Time stepping in a local frame"},{"category":"section","location":"methodology/#Initial-guess","page":"Methodology","text":"The implicit solves are seeded by extrapolating from the two previous steps. That extrapolation is the other place where clock precision leaks into the result, and the fix there is sharper than the local time frame — it removes the clock from the guess altogether.\n\nUpstream, the implicit partitioned Runge–Kutta integrator builds an absolute time for stage i,\n\nt = history[1].t + timestep(int) * tableau(int).q.c[i]\n\nand hands it to HermiteExtrapolation, which immediately differences it back down to a normalised one (Δt = t₁ - t₀, s = (tᵢ - t₀) / Δt). Every quantity in that round trip is stored in the working precision, so c → t → c loses accuracy in half precision — and once the clock saturates it fails outright with t₀ == t₁.\n\nBut the normalised time is a tableau constant: stage i sits c[i] steps past the previous step by construction. NormalizedHermiteExtrapolation accepts that constant directly and never looks at a clock, so src/initial_guess.jl overrides initial_guess! to call extrapolate! with tableau(int).q.c[i] instead of routing through solutionstep!. The guess then involves no time arithmetic whatsoever, at any precision.\n\nThe effect is measurable: with this guess the partitioned Runge–Kutta methods become entirely clock-independent. At BFloat16, Δt = 0.1, t ≤ 100 — well past the t ≈ 16 saturation point — localclock = true and localclock = false produce bit-identical results.\n\nThe two mechanisms have complementary jobs, and it is worth being precise about which does what. On the four Hamiltonian problems every method is either explicit (and so has no initial guess to spoil) or a partitioned Runge–Kutta method whose guess is clock-free — so there the tableau-driven guess alone suffices and the local frame is redundant. The local frame is what carries the Lotka–Volterra (IODE/LODE) integrators, whose own initial_guess! methods difference absolute times. It is applied unconditionally because it is free, and because it protects any method added later that has not been given a clock-free guess.\n\nTwo implementation notes. NormalizedHermiteExtrapolation expects derivative samples scaled by Δt and returns a Δt-scaled derivative, while the integrator cache holds unscaled vector-field values, so the scaling is applied on the way in and undone on the way out. The two formulations agree exactly in exact arithmetic, and the extrapolated state agrees to round-off at every precision; only the extrapolated derivative rounds differently, because the Hermite derivative basis differences two terms of size 6c(1+c)·x/Δt and both formulations lose accuracy to that cancellation in half precision. That is harmless — an initial guess can only change how many iterations a step needs, never what it converges to. And the override necessarily applies to every implicit partitioned RK method rather than only the two Gauss rules: initmethod collapses them all into the same IPRK integrator type, so the originating method cannot be recovered at dispatch time. That is the coherent choice regardless, since Gauss(2) and PRK Gauss(2) are the same integrator here and would otherwise be seeded differently.\n\nNormalizedHermiteExtrapolation requires GeometricIntegratorsBase ≥ v0.4.2; the package's [compat] bound for that dependency is tighter (v0.5.1), set by the next section.","title":"Initial guess"},{"category":"section","location":"methodology/#Solver-tolerances","page":"Methodology","text":"The same \"is this quantity really at the working precision?\" question applies to the nonlinear solve's convergence criterion, which is assessed as\n\nrfₐ ≤ f_abstol + f_reltol · ‖F(x₀)‖\n\nrun_study and integrate_bounded pass no tolerances of their own; every term above has to be scaled by the stack, and each is. SimpleSolvers builds its Options from the state's element type, giving x_abstol = 2eps(T) and f_reltol = √eps(T), and GeometricIntegratorsBase.default_options supplies the absolute term as\n\nf_abstol = max(8, solversize(method, problem)) · eps(datatype(problem))\n\nscaled to the working precision and to the size of the stage system. Caller options are merged into that bundle rather than replacing it, so overriding one keyword does not silently drop min_iterations = 1.\n\nBoth factors are load-bearing, and for different reasons.\n\nThe precision factor. The better the initial guess, the smaller ‖F(x₀)‖ and the more the absolute term alone decides the test — so an improved guess makes convergence harder to certify, which is the wrong way round and is what makes a mis-scaled floor bite here in particular. A half-precision residual bottoms out near eps(T), so an absolute floor fixed at 8eps(Float64) ≈ 1.8e-15 is simply unreachable: every implicit solve then runs to its iteration limit, on every step, for a result that was already as good as the arithmetic allows. The partitioned problems of this study report solversize = 0 and so sit on the max(8, …) floor at 8eps(T), which is the value the argument above asks for at each precision.\n\nThe size factor. The Gauss(8) reference integrations are Float64, so 8eps() is the \"right\" value for them by the same argument — and for four of the five it is: the pendulum (16 unknowns), the double pendulum (32), the Toda lattice (256) and the 2D Lotka–Volterra system (16) all reach it with nothing capped. The 4D Lotka–Volterra system does not. It is the degenerate one, posed with the quasi-canonical reduced gauge matrix, and its residual bottoms out just above that threshold — so the obstruction is conditioning, not dimension. The size factor nevertheless covers it: at 32 unknowns it gives 32eps ≈ 7.1e-15, roughly a 4× loosening, and the reference converges in a handful of iterations. Scaling by the stage size is dimensionally the right thing anyway, the residual being an l2 norm over n components whose own round-off grows with n.\n\nThe failure mode this avoids is worth naming, because it is silent: a non-convergent solve is only a warning, so an unreachable floor lets a reference solution be used without having met its convergence criterion.\n\nSimpleSolvers supplies the complementary exit. assess_convergence cannot certify a solve that has converged as far as its arithmetic allows but cannot say so, so a solve that stops making progress is stopped instead: after max_stalls = 2 stalled steps it gives up with one actionable message. A line search is bounded by its own linesearch_max_iterations rather than by max_iterations, and shares its solver's Options — which is what lets verbosity = 0 reach it.\n\nsolveropts is the escape hatch: any SimpleSolvers.Options keywords, applied to every run or resolved per precision by a callable T -> NamedTuple. (verbosity = 0,) is the useful one here, silencing a configuration that is expected to fail.","title":"Solver tolerances"},{"category":"section","location":"methodology/#Type-purity-verification","page":"Methodology","text":"Precision is set by the problem in two independent type parameters: the data type (datatype, from eltype(q₀)) drives the state and cache allocations, and the time type (timetype, from promote(t₀, t₁, Δt)) drives the tableau coefficients. To stay pure in T, both the initial conditions and the timespan/timestep must be T.\n\nFor every successful run, verify_precision asserts that\n\ndatatype(prob) === timetype(prob) === T,\ndatatype(sol) === timetype(sol) === T, and\nthe element type of the stored q and p arrays is T,\n\nwhich proves that no library in the stack silently promotes to Float64. Integration failures (e.g. half-precision blow-ups) are caught per run so a single failure never aborts the sweep.","title":"Type-purity verification"},{"category":"section","location":"methodology/#Error-metrics","page":"Methodology","text":"Relative energy error |(H(qₙ, pₙ) − H₀) / H₀|, computed in the run's own precision.\nSolution error — the Euclidean norm of the state difference against the reference solution. For the problems without an analytic solution (all but the harmonic oscillator), the coarse-step scenarios compute the Float64 Gauss(8) reference at the fine step (the short scenario's Δt) and subsample it onto the coarse output grid, so the reference is accurate independent of the coarse step. (The short scenarios' reference already uses the fine step, being run at it.)","title":"Error metrics"},{"category":"section","location":"methodology/#Plotting-conventions","page":"Methodology","text":"Every figure has one panel per precision (BFloat16 / Float16 / Float32 / Float64, coarsest first) and is produced once per method group. The four Hamiltonian problems use three groups — Euler (_euler), other (_other), and the partitioned Gauss(2) variants (_gauss2); the Lotka–Volterra problems use a single variational group (_variational). Scripts pass their group set to the plotting routines, which colour methods consistently within it.\nError plots use a shared logarithmic y-axis across all four panels, with the upper limit capped at 1e5 so runaway (non-geometric) errors are clipped rather than dominating the scale; the x-axis is fitted exactly to the integration interval.\nSolution plots show the 2D trajectory of each method (phase space (q, p), or configuration space for multi-degree-of-freedom systems) with the reference drawn as a black backdrop.\nColours are consistent per method; the legend is a horizontal row beneath the panels.","title":"Plotting conventions"},{"category":"section","location":"pendulum/#Pendulum","page":"Pendulum","text":"The mathematical pendulum H(qp) = p^22 + cos q is nonlinear and has no closed-form solution, so the solution error is measured against a high-precision Float64 Gauss(8) reference (in the coarse scenario computed at the fine step and subsampled to the output grid). All methods are type-pure at all four precisions and run the full horizon.","title":"Pendulum"},{"category":"section","location":"pendulum/#Short-scenario-(Δt-0.1,-t-1000)","page":"Pendulum","text":"","title":"Short scenario (Δt = 0.1, t ≤ 1000)"},{"category":"section","location":"pendulum/#Energy-error","page":"Pendulum","text":"(Image: Energy error, Euler methods)\n\n(Image: Energy error, other methods)\n\n(Image: Energy error, Gauss(2) variants)\n\nAs for the harmonic oscillator, the symplectic methods keep the energy error bounded while explicit Euler grows and implicit Euler dissipates. The two orders separate instructively here. Implicit midpoint sits at ≈ 2e-1 / 4e-2 / 6e-4 / 6e-4 for BFloat16 / Float16 / Float32 / Float64 — the last two are equal, because at Δt = 0.1 this second-order rule is already truncation-limited on the nonlinear pendulum, so precision beyond Float32 buys it nothing. Implicit RK4, being fourth-order, is still round-off-limited at those precisions and keeps improving (≈ 2e-5 at Float32, 3e-7 at Float64). Which of the two floors a curve is sitting on — arithmetic or truncation — is therefore method-dependent, and reading a precision study requires knowing which. The four partitioned-Gauss(2) variants (symplectic-by-construction vs. by-duplication, with and without the rounding-compensation coefficients â b̂ ĉ) become distinguishable here: on this nonlinear system the implementation differences are visible in the energy-error fine structure.","title":"Energy error"},{"category":"section","location":"pendulum/#Solution-error","page":"Pendulum","text":"(Image: Solution error, Euler methods)\n\n(Image: Solution error, other methods)\n\n(Image: Solution error, Gauss(2) variants)\n\nAgainst the Gauss(8) reference the trajectory error accumulates fastest for the non-symplectic Euler and explicit-midpoint methods, while the symplectic, implicit-midpoint and Gauss(2) methods keep the error small; reduced precision raises the floor without reordering the methods.","title":"Solution error"},{"category":"section","location":"pendulum/#Phase-space-trajectory","page":"Pendulum","text":"(Image: Phase-space trajectory, Euler methods)\n\n(Image: Phase-space trajectory, other methods)\n\n(Image: Phase-space trajectory, Gauss(2) variants)","title":"Phase-space trajectory"},{"category":"section","location":"pendulum/#Long-scenario-(Δt-1,-t-10-000)","page":"Pendulum","text":"","title":"Long scenario (Δt = 1, t ≤ 10 000)"},{"category":"section","location":"pendulum/#Energy-error-2","page":"Pendulum","text":"(Image: Energy error, Euler methods)\n\n(Image: Energy error, other methods)\n\n(Image: Energy error, Gauss(2) variants)\n\nThe symplectic and midpoint/trapezoidal methods — together with the partitioned Gauss(2) variants — remain bounded over the full horizon; the non-symplectic Euler and explicit-midpoint methods drift. Both half precisions carry the full t ≤ 10 000 horizon here, far past where a T-typed clock stops advancing (t ≈ 2048 in Float16, t ≈ 256 in BFloat16), which on its own would make the implicit methods fail outright — see Time stepping in a local frame and Initial guess. This scenario also holds the only remaining failure among the four Hamiltonian problems: Implicit Euler at Float16 throws a NaN in the Newton direction. A first-order dissipative method at Δt = 1 is the least promising combination in the study, so it is a fitting place for the last one to sit.","title":"Energy error"},{"category":"section","location":"pendulum/#Solution-error-2","page":"Pendulum","text":"(Image: Solution error, Euler methods)\n\n(Image: Solution error, other methods)\n\n(Image: Solution error, Gauss(2) variants)\n\nOver the long horizon the drifting methods depart from the reference, whereas the bounded geometric and Gauss(2) methods keep a controlled trajectory error at the precisions where they still run.","title":"Solution error"},{"category":"section","location":"pendulum/#Phase-space-trajectory-2","page":"Pendulum","text":"(Image: Phase-space trajectory, Euler methods)\n\n(Image: Phase-space trajectory, other methods)\n\n(Image: Phase-space trajectory, Gauss(2) variants)","title":"Phase-space trajectory"},{"category":"section","location":"toda_lattice/#Toda-Lattice","page":"Toda Lattice","text":"The Toda lattice is a periodic N-site lattice with nearest-neighbour exponential interactions H(qp) = tfrac12 p cdot p + alpha sum_n e^q_n - q_n+1 — a classic completely integrable system. Like the double pendulum it is EulerLagrange-generated (no ::Type{T} constructor, inputs built at precision T by hand), and its Hamiltonian additionally takes the lattice size N. A modest lattice (N = 16) is used so the sweep stays tractable; the default N = 200 soliton example would make the high-order reference and the implicit solves prohibitively slow. The trajectory plots use the phase space (q_1 p_1) of the first lattice site, and the bump initial condition keeps q in 01 so the exponentials are well-behaved.","title":"Toda Lattice"},{"category":"section","location":"toda_lattice/#Short-scenario-(Δt-0.1,-t-100)","page":"Toda Lattice","text":"","title":"Short scenario (Δt = 0.1, t ≤ 100)"},{"category":"section","location":"toda_lattice/#Energy-error","page":"Toda Lattice","text":"(Image: Energy error, Euler methods)\n\n(Image: Energy error, other methods)\n\n(Image: Energy error, Gauss(2) variants)\n\nAll methods run at all four precisions — the bounded initial data makes the Toda lattice markedly more half-precision-friendly than the double pendulum. The now-familiar pattern holds: symplectic and midpoint/trapezoidal methods keep the energy error bounded (≈ 1e-5–1e-6 at Float32/Float64), while explicit midpoint and RK4 drift and the Euler methods grow/dissipate. Among the four partitioned-Gauss(2) variants the differences between the symplectic and duplicated tableaus and between keeping or zeroing â b̂ ĉ are most visible at the higher precisions where the energy-error floor is not precision-limited.","title":"Energy error"},{"category":"section","location":"toda_lattice/#Solution-error","page":"Toda Lattice","text":"(Image: Solution error, Euler methods)\n\n(Image: Solution error, other methods)\n\n(Image: Solution error, Gauss(2) variants)\n\nAgainst the Gauss(8) reference the first-site trajectory error stays smallest for the symplectic, midpoint and Gauss(2) methods and largest for the non-geometric Euler / explicit-midpoint methods, tracking the energy-error ranking across all four precisions.","title":"Solution error"},{"category":"section","location":"toda_lattice/#Phase-space-trajectory","page":"Toda Lattice","text":"(Image: Phase-space trajectory, Euler methods)\n\n(Image: Phase-space trajectory, other methods)\n\n(Image: Phase-space trajectory, Gauss(2) variants)\n\nThe first site traces a quasi-periodic orbit; the symplectic methods stay on it while explicit Euler spirals out and implicit Euler spirals in, just as for the single oscillator.","title":"Phase-space trajectory"},{"category":"section","location":"toda_lattice/#Coarse-scenario-(Δt-1,-t-100)","page":"Toda Lattice","text":"","title":"Coarse scenario (Δt = 1, t ≤ 100)"},{"category":"section","location":"toda_lattice/#Energy-error-2","page":"Toda Lattice","text":"(Image: Energy error, Euler methods)\n\n(Image: Energy error, other methods)\n\n(Image: Energy error, Gauss(2) variants)\n\nThe Gauss(8) reference converges even at Δt = 1, so the full plot set is produced. At this coarse step the explicit methods diverge within the first ~10–15 steps (guard-truncated) at every precision, while implicit midpoint and implicit RK4 keep the energy error near machine level (≈ 1e-5 in Float32/Float64, sitting at the resolution floor in the two half precisions) over the whole horizon and explicit RK4 drifts only mildly. This is the one scenario where the clock was never the binding constraint in the first place: at Δt = 1 a BFloat16 clock only runs out at t ≈ 256, comfortably past this t ≤ 100 horizon.","title":"Energy error"},{"category":"section","location":"toda_lattice/#Solution-error-2","page":"Toda Lattice","text":"(Image: Solution error, Euler methods)\n\n(Image: Solution error, other methods)\n\n(Image: Solution error, Gauss(2) variants)\n\nThe guard-truncated explicit methods lose the reference within the first few steps, while the implicit-midpoint, implicit-RK4 and Gauss(2) methods keep the coarse-step trajectory error controlled over the full horizon.","title":"Solution error"},{"category":"section","location":"toda_lattice/#Phase-space-trajectory-2","page":"Toda Lattice","text":"(Image: Phase-space trajectory, Euler methods)\n\n(Image: Phase-space trajectory, other methods)\n\n(Image: Phase-space trajectory, Gauss(2) variants)","title":"Phase-space trajectory"},{"category":"section","location":"lotka_volterra_2d/#Lotka–Volterra-2D","page":"Lotka–Volterra 2D","text":"The Lotka–Volterra predator–prey system is a degenerate Lagrangian system: its Lagrangian is linear in the velocities, so it has no ordinary Hamiltonian phase-space form and is posed instead as an implicit / degenerate-Lagrangian (IODE / LODE) problem. This example uses the LotkaVolterra2dSingular module — the singular gauge of the Lagrangian, which is the one the CMDVI degenerate variational integrator requires.\n\nBecause it is degenerate, the explicit, symplectic-Euler and diagonally-implicit Runge–Kutta methods of the other examples do not apply. The comparison here is instead between four flavours of the implicit midpoint rule that do apply to such systems and differ only in implementation detail:\n\nImplicit Midpoint — the plain 1-stage Gauss / midpoint Runge–Kutta rule;\nVPRK Gauss(1) — the variational partitioned Runge–Kutta form of the same rule;\nPMVI Midpoint — the position–momentum variational integrator;\nCMDVI — the continuous midpoint degenerate variational integrator.\n\nThere is no closed-form solution; the solution error is measured against a Float64 Gauss(8) reference (in the coarse scenario computed at the fine step and subsampled to the output grid). All four methods are type-pure at every precision.","title":"Lotka–Volterra 2D"},{"category":"section","location":"lotka_volterra_2d/#Short-scenario-(Δt-0.01,-t-10)","page":"Lotka–Volterra 2D","text":"","title":"Short scenario (Δt = 0.01, t ≤ 10)"},{"category":"section","location":"lotka_volterra_2d/#Energy-error","page":"Lotka–Volterra 2D","text":"(Image: Energy error, variational integrators)\n\nAt Float32 and Float64 all four midpoint variants conserve energy well and trace visibly different fine structure — CMDVI in particular follows a distinct curve. The half precisions are where this problem class parts company with the Hamiltonian ones: the variational solves hit a NaN-direction breakdown in the nonlinear solver, VPRK and PMVI at both 16-bit formats and CMDVI additionally at BFloat16.\n\nThis is the one family in the study where — unlike the failures on the Hamiltonian problems — the breakdown is not a bookkeeping artefact. These are singular (degenerate-Lagrangian) systems whose one-form involves log q, so the nonlinear iterate has to stay in the positive orthant; at 8 significand bits it does not. Neither the local time frame nor the tableau-driven initial guess helps here — the latter targets the PODE/HODE code path, and these problems are LODEs.","title":"Energy error"},{"category":"section","location":"lotka_volterra_2d/#Solution-error","page":"Lotka–Volterra 2D","text":"(Image: Solution error, variational integrators)\n\nAgainst the Gauss(8) reference the four variants keep a small trajectory error at Float32/Float64, with CMDVI again separating from the others; in the half precisions only the surviving methods contribute.","title":"Solution error"},{"category":"section","location":"lotka_volterra_2d/#Configuration-space-trajectory","page":"Lotka–Volterra 2D","text":"(Image: Trajectory, variational integrators)","title":"Configuration-space trajectory"},{"category":"section","location":"lotka_volterra_2d/#Coarse-scenario-(Δt-0.1,-t-100)","page":"Lotka–Volterra 2D","text":"","title":"Coarse scenario (Δt = 0.1, t ≤ 100)"},{"category":"section","location":"lotka_volterra_2d/#Energy-error-2","page":"Lotka–Volterra 2D","text":"(Image: Energy error, variational integrators)\n\nAt the coarser step and longer horizon the differences between the variational integrators, and the effect of precision on their long-time energy behaviour, are accentuated.","title":"Energy error"},{"category":"section","location":"lotka_volterra_2d/#Solution-error-2","page":"Lotka–Volterra 2D","text":"(Image: Solution error, variational integrators)\n\nOver the longer horizon the trajectory error against the reference likewise separates the variants, exposing how each variational integrator accumulates error at the coarse step.","title":"Solution error"},{"category":"section","location":"lotka_volterra_2d/#Configuration-space-trajectory-2","page":"Lotka–Volterra 2D","text":"(Image: Trajectory, variational integrators)","title":"Configuration-space trajectory"},{"category":"section","location":"findings/#Findings","page":"Findings","text":"","title":"Findings"},{"category":"section","location":"findings/#Geometric-vs.-non-geometric-integrators","page":"Findings","text":"Across the four Hamiltonian problems and both scenarios the qualitative distinction is consistent and matches the theory of geometric numerical integration (the two degenerate-Lagrangian Lotka–Volterra problems use a separate variational-integrator comparison, discussed below):\n\nSymplectic methods (symplectic Euler A/B, implicit midpoint) keep the energy error bounded — it oscillates around a small value rather than growing — over arbitrarily long integrations. In phase space their trajectories stay on a closed level set of a nearby modified Hamiltonian.\nExplicit Euler systematically increases the energy (spirals outward in phase space); implicit Euler systematically dissipates it (spirals inward). Both are unusable for long-time dynamics regardless of precision.\nThe Gauss collocation rules — implicit midpoint (Gauss(1), order 2) and implicit RK4 (Gauss(2), order 4) — being symplectic and symmetric, nearly conserve energy, with a bounded error set by the precision.\nExplicit RK4 is accurate over short times but, being non-symplectic, exhibits a slow energy drift over long horizons. Set against implicit RK4 at the same order, this isolates the effect of symplecticity from the effect of accuracy: the two track each other closely at first, then separate once the drift accumulates, while their truncation errors remain comparable.","title":"Geometric vs. non-geometric integrators"},{"category":"section","location":"findings/#The-role-of-precision","page":"Findings","text":"For the energy-conserving methods the precision sets the floor of the (bounded) energy error. Measured for the implicit midpoint rule on the harmonic oscillator at Δt = 0.1 over t ≤ 1000 (mean over the second half of the run):\n BFloat16 Float16 Float32 Float64\nharmonic oscillator 1.1e-2 8.9e-3 2.1e-6 5.4e-15\npendulum 2.5e-1 3.7e-2 6.2e-4 6.2e-4\nThe other problems show the same ordering.\nThe floor is not always round-off. On the pendulum the implicit midpoint rule reaches the same 6.2e-4 at Float32 and Float64: there the error is set by the method's truncation error at Δt = 0.1, not by the arithmetic, so extra precision buys nothing. The SPRK Gauss(2) rule, being of higher order, does keep improving (2.2e-5 → 2.7e-7). Reading a precision study therefore requires knowing which of the two floors a given curve is sitting on.\nFor the low-order methods the solution error is likewise often dominated by truncation rather than round-off, so Float32 and Float64 solution errors can be nearly identical while only the half precisions show a round-off floor.\nReducing precision therefore mainly matters for (a) the achievable energy-conservation floor and (b) the robustness of the implicit solves and the time grid — not so much for the truncation- limited accuracy of the low-order methods.","title":"The role of precision"},{"category":"section","location":"findings/#BFloat16-versus-Float16:-exponent-range-is-the-wrong-trade","page":"Findings","text":"BFloat16 and Float16 are both 16 bits wide and differ only in how they split them. BFloat16 carries Float32's 8-bit exponent, so it reaches ≈ 3.4e38 instead of 65504, and pays for that with three significand bits (eps 2⁻⁷ ≈ 7.8e-3 against 2⁻¹⁰ ≈ 9.8e-4).\n\nFor the problems studied here that is a bad trade in both directions:\n\nThe extra range is never used. These are bounded Hamiltonian systems; the state stays O(1) and the only thing that ever reaches Float16's 65504 ceiling is a method that has already blown up (explicit Euler at a coarse step), which the divergence guard stops anyway. BFloat16 merely lets such a run climb further before being cut off — visible as the explicit-Euler curves running higher up the clipped 1e5 ceiling.\nThe missing significand bits cost accuracy directly. BFloat16's bounded energy-error floor sits a factor of ≈ 8 above Float16's — precisely the ratio of their eps — and a BFloat16 time grid saturates eight times sooner.\n\nSo BFloat16 is the weakest of the four precisions here, and predictably so. This is not an argument against the format — its range is what makes it useful for the wide dynamic ranges of neural-network training — but it does mean that a format chosen for machine learning is not automatically a good fit for geometric integration, where accuracy per bit is what matters.","title":"BFloat16 versus Float16: exponent range is the wrong trade"},{"category":"section","location":"findings/#Type-purity","page":"Findings","text":"verify_precision passes for every successful run across all six problems and all four precisions: datatype, timetype, and the element types of the stored q/p arrays all equal the requested precision. No library in the stack (GeometricIntegrators, GeometricIntegratorsBase, GeometricSolutions, GeometricEquations, GeometricBase, SimpleSolvers) silently promotes to Float64, including for the hand-built half-precision constructions of the double pendulum and Toda lattice. The key requirement is that both the initial conditions and the timespan/timestep are created at the target precision, so that the data type and the time type agree.\n\nBFloat16 is the sharpest test of this, because its arithmetic is implemented by widening to Float32 and rounding back. The gate confirms that the widening never escapes into the stored state: every q, p and time value comes back as BFloat16. The generic (non-BLAS) linear algebra the implicit solvers need — lu, \\, norm, dot — works on BFloat16 unchanged.","title":"Type purity"},{"category":"section","location":"findings/#The-reduced-precision-clock,-and-why-it-is-not-a-real-limit","page":"Findings","text":"The most conspicuous half-precision failure mode in this study is an artefact of bookkeeping rather than a property of the integrators.\n\nA time variable stored in T stops advancing once ulp(t) ≥ Δt: successive stamps round to the same value, and the implicit methods' HermiteExtrapolation — which divides by t₁ - t₀ — fails with t₀ == t₁. The onset arrives early, and eight times earlier for BFloat16 than for Float16 (t ≈ 16 vs t ≈ 128 at Δt = 0.1). Capping every horizon there would leave almost nothing to study.\n\nBut no method here derives its step size from a difference of clock values — they all read Δt from the problem — so absolute time only ever reaches the vector field, and all six problems are autonomous. Advancing each step in a local time frame is therefore exact, and it removes the limit entirely: every precision runs every horizon. Against stepping along the problem's own grid (localclock = false) the explicit methods are bit-identical; the implicit ones differ only at solver tolerance.\n\nThe same reasoning applied once more gives a sharper result still. The upstream initial guess reaches its interpolation node by building an absolute stage time and differencing it back down — even though that node is a tableau constant (c[i]) that needs no clock at all. Feeding the constant directly to NormalizedHermiteExtrapolation (see Initial guess) makes the partitioned Runge–Kutta methods entirely clock-independent: at BFloat16 past the saturation point, stepping along the saturating grid and stepping in the local frame give bit-identical results. Where the local frame works around the coarse clock, this removes the dependence on it.\n\nThe third instance sits in the nonlinear solve, and this one is not confined to half precision. Its convergence test is rfₐ ≤ f_abstol + f_reltol·‖F(x₀)‖ (see Solver tolerances), and an f_abstol pinned to 8eps(Float64) regardless of the run's precision is unsatisfiable: a half-precision residual bottoms out near eps(T), so every implicit solve burns its full 1000-iteration budget on every step without ever being reported as non-convergent. Scaling the tolerance to the run's own precision leaves Float64 results bit-identical and the rest unchanged to round-off, and makes the sweep 2.6–40× faster.\n\nThe Float64 reference integrations have the same disease from a different cause. The 4D Lotka–Volterra Gauss(8) reference cannot reach 8eps(Float64) either — not because of precision but because of conditioning — and so exhausts all 1000 iterations on every step, silently, for a solution the study treats as ground truth. Scaling the floor with the stage size covers it: two to three orders of magnitude faster, and the loosened and capped solutions agree to 1.4e-12, so the capped solve had converged all along without being able to certify it. Note the perverse coupling: the relative term is measured at the initial guess, so the better the extrapolation, the smaller ‖F(x₀)‖ and the more the test collapses onto the bare absolute floor — improving the guess makes convergence harder to certify.\n\nBoth factors are supplied by the stack itself: GeometricIntegratorsBase defaults f_abstol to max(8, solversize(method, problem)) · eps(datatype(problem)), precision- and size-scaled at once, and SimpleSolvers gives a stalling solve the stagnation exit that the residual test cannot provide. The study passes no tolerances of its own.\n\nFive lessons generalise beyond this repository:\n\nDistinguish the precision of the state from the precision of the bookkeeping. They fail at very different horizons, and conflating them makes a solvable problem look like a hard limit.\nPrefer the invariant quantity to the reconstructed one. All three instances have the same shape: something already known exactly — the timestep, a tableau node, the machine epsilon of the working type — gets reconstructed from accumulated absolute values instead of used directly. In Float64 the difference is invisible; in half precision it is the whole story.\nAudit the tolerances too, not just the arrays. A type-purity gate on the state will not catch a convergence threshold written for Float64. Asking a BFloat16 solve for a Float64 residual is not a stricter test, just an unsatisfiable one — and the Float64 reference shows the same thing can happen from conditioning alone.\nTreat \"did not converge\" as a result, not a warning. Each of these degrades accuracy or wastes most of the compute while emitting nothing worse than a log line, so it can run unnoticed for a long time. A solve that exhausts its iteration budget on every step should be as visible as one that throws.\nA \"half precision breaks the solver\" symptom may be an accumulated-time problem. On the double pendulum a global clock costs the Float16 implicit solves a NaN in the Newton direction, and swapping the initial-guess extrapolation only papers over it. The cause is that Hermite reads its interval off the stored history times, and Float16 resolves only about 0.004 near t = 5 — comparable to Δt = 0.01 — so the extrapolation parameter goes materially wrong long before the clock saturates outright. In the local frame it is exact and the NaNs do not occur at any precision, so the default HermiteExtrapolation is the better guess everywhere.","title":"The reduced-precision clock, and why it is not a real limit"},{"category":"section","location":"findings/#Genuine-limits-of-half-precision-(not-bugs)","page":"Findings","text":"What remains once the clock is out of the way is a much shorter list, and it is real:\n\nThe round-off floor on the achievable energy error. This is the honest, unavoidable cost of reduced precision, and the one the study is really about: the bounded energy error of a symplectic method cannot go below what the working precision can represent.\nOne non-convergent solve on the Hamiltonian problems. Across all eight Hamiltonian runs — every method at every precision — exactly one integration fails: Implicit Euler at Float16 on the coarse-step pendulum (Δt = 1, t ≤ 10 000), with a NaN in the Newton direction. That is a first-order dissipative method at a very coarse step, i.e. the case with the least to recommend it anyway. Everything else completes, at all four precisions, over the full horizon.\nThe degenerate-Lagrangian variational integrators break down in half precision. Across the four Lotka–Volterra runs, VPRK Gauss(1) fails with a NaN in the nonlinear-solver direction in every scenario at both 16-bit formats, and PMVI Midpoint in all but one; CMDVI and plain implicit midpoint each fail in one scenario. These are singular systems whose one-form involves log(q), so the nonlinear iterate has to stay in the positive orthant — and 8–11 significand bits are not enough to keep it there. The iterate steps to a negative coordinate, the logarithm is evaluated there, and because GeometricProblems generates its vector fields with nanmath = true the model returns NaN rather than throwing a DomainError — so the breakdown surfaces one level up, in the solver. This is the one family where the failures are not a clock or tolerance artefact, and notably it is not a clean BFloat16-versus-Float16 story: the two 16-bit formats fail on overlapping but not identical sets, which is what one should expect when the breakdown turns on whether a particular iterate happens to step outside the domain.\nProblem-dependent robustness. The Toda lattice, whose bump initial data keeps the state bounded, runs every method at every precision — considerably more half-precision-friendly than the stiff, dimensional double pendulum.\n\nAll such failures are caught per run, reported as skips, and never abort the sweep.","title":"Genuine limits of half precision (not bugs)"},{"category":"section","location":"findings/#Implementation-detail-and-variational-comparisons","page":"Findings","text":"Two further comparison groups isolate finer effects:\n\nPartitioned Gauss(2) variants. On the Hamiltonian problems, four algebraically equivalent forms of the 2-stage Gauss rule (symplectic-by-construction vs. by-duplication, with and without the rounding-compensation coefficients â, b̂, ĉ) coincide on the linear harmonic oscillator but separate on the nonlinear problems, where the tableau construction and the compensated-summation coefficients leave a visible imprint on the energy-error fine structure — most so at Float64, where the floor is not precision-limited.\nVariational integrators on degenerate Lagrangians. The Lotka–Volterra systems are compared with several flavours of the implicit midpoint rule that apply to degenerate (IODE/LODE) systems. They agree to their common order but differ in reduced-precision energy behaviour; CMDVI integrates the 2D system but not the 4D one (its iterate leaves the positive orthant and the solve breaks down there).","title":"Implementation-detail and variational comparisons"},{"category":"section","location":"findings/#Practical-takeaways","page":"Findings","text":"Use a symplectic (or at least symmetric) integrator whenever long-time energy behaviour matters; the qualitative advantage is independent of precision, and holds all the way down to BFloat16.\nFloat32 is a reasonable working precision for these problems: it preserves the geometric behaviour with a modest error floor and is far more robust than either 16-bit format.\nHalf precision is more usable than it first appears, provided the clock, the initial-guess node and the solver tolerance are not themselves tied to it. With those separated out, both 16-bit formats run every method over every horizon studied on the four Hamiltonian problems; what they cost there is accuracy (an error floor of 1e-3–1e-1) and, on chaotic systems, predictability horizon. The degenerate-Lagrangian problems are the exception, and BFloat16 genuinely fails on them.\nBetween the two 16-bit formats, prefer Float16 for this kind of work. BFloat16's wider exponent range is not useful for bounded Hamiltonian systems, and the three significand bits it costs show up directly in the error floor and in how early the time grid saturates.\nDo not store accumulating quantities at the working precision by default. Time is the example here, but the lesson is general: a monotonically growing counter in BFloat16 runs out after ~256 increments, long before the state precision becomes the binding constraint.","title":"Practical takeaways"},{"category":"section","location":"harmonic_oscillator/#Harmonic-Oscillator","page":"Harmonic Oscillator","text":"The harmonic oscillator H(qp) = p^2(2m) + k q^2  2 is the reference test case: it is linear, it has a closed-form solution (so the solution error is measured against the analytic solution), and it is where the type-purity pipeline was first verified. All twelve methods (the eight Euler/Runge–Kutta methods plus the four partitioned Gauss(2) variants) run at all four precisions, over the full horizon, and pass the precision-purity gate.","title":"Harmonic Oscillator"},{"category":"section","location":"harmonic_oscillator/#Short-scenario-(Δt-0.1,-t-1000)","page":"Harmonic Oscillator","text":"","title":"Short scenario (Δt = 0.1, t ≤ 1000)"},{"category":"section","location":"harmonic_oscillator/#Energy-error","page":"Harmonic Oscillator","text":"(Image: Energy error, Euler methods)\n\n(Image: Energy error, other methods)\n\n(Image: Energy error, Gauss(2) variants)\n\nThe qualitative picture is textbook: explicit Euler grows without bound, implicit Euler dissipates towards a constant relative error of order one, and the symplectic Euler methods keep a bounded, oscillating energy error. Among the midpoint / fourth-order group, the two implicit (Gauss) rules nearly conserve energy, with their noise floor set by the precision — implicit midpoint sits at ≈ 1e-2 for BFloat16, 9e-3 for Float16, 2e-6 for Float32 and 5e-15 for Float64 — while their explicit counterparts drift. Because that group is a 2 × 2 (explicit vs. implicit at order 2 and at order 4), the two effects are separable in one figure: moving from explicit to implicit changes the qualitative behaviour from drift to bounded, while raising the order lowers the floor within each. The two 16-bit panels differ by the factor ≈ 8 between their eps values — the round-off floor tracks significand bits, and nothing else. The four partitioned Gauss(2) variants differ only in implementation detail — symplectic-by-construction (SPRK) versus by-duplication (PRK), and whether the rounding-error compensation coefficients â b̂ ĉ are retained or zeroed; on this linear system the four are almost indistinguishable, and the differences grow on the nonlinear problems.","title":"Energy error"},{"category":"section","location":"harmonic_oscillator/#Solution-error","page":"Harmonic Oscillator","text":"(Image: Solution error, Euler methods)\n\n(Image: Solution error, other methods)\n\n(Image: Solution error, Gauss(2) variants)\n\nMeasured against the analytic solution, the trajectory error mirrors the energy behaviour: the symplectic Euler methods keep a bounded, oscillating error (≈ 1e-2), explicit Euler grows and implicit Euler saturates near order one, while the higher-order and Gauss(2) methods stay well below the Euler pair. Reduced precision mainly raises the noise floor rather than changing the ranking.","title":"Solution error"},{"category":"section","location":"harmonic_oscillator/#Phase-space-trajectory","page":"Harmonic Oscillator","text":"(Image: Phase-space trajectory, Euler methods)\n\n(Image: Phase-space trajectory, other methods)\n\n(Image: Phase-space trajectory, Gauss(2) variants)\n\nIn phase space the behaviour is unmistakable: the symplectic methods stay on a closed (slightly deformed) ellipse — the level set of a nearby modified Hamiltonian — the reference is the exact circle, explicit Euler spirals outward, and implicit Euler spirals inward.","title":"Phase-space trajectory"},{"category":"section","location":"harmonic_oscillator/#Long-scenario-(Δt-1,-t-10-000)","page":"Harmonic Oscillator","text":"","title":"Long scenario (Δt = 1, t ≤ 10 000)"},{"category":"section","location":"harmonic_oscillator/#Energy-error-2","page":"Harmonic Oscillator","text":"(Image: Energy error, Euler methods)\n\n(Image: Energy error, other methods)\n\n(Image: Energy error, Gauss(2) variants)\n\nAt the coarse step and long horizon the contrast is dramatic: explicit Euler and explicit midpoint diverge exponentially (reaching ≈ 1e300 in Float64, clipped at the plot's 1e5 ceiling), while the symplectic methods and the Gauss collocation rules — and the partitioned Gauss(2) variants — remain bounded over the entire 10^4 time units, at every precision. This horizon is far beyond where a T-typed clock stops advancing in half precision (t ≈ 2048 in Float16, t ≈ 256 in BFloat16 at this Δt), which on its own would make the implicit methods fail outright. The local time frame and the tableau-driven initial guess between them remove that limit — for the methods here it is really the initial guess that does the work, since every method in this study is either explicit (and so has no initial guess to spoil) or a partitioned Runge–Kutta method whose guess is clock-free.","title":"Energy error"},{"category":"section","location":"harmonic_oscillator/#Solution-error-2","page":"Harmonic Oscillator","text":"(Image: Solution error, Euler methods)\n\n(Image: Solution error, other methods)\n\n(Image: Solution error, Gauss(2) variants)\n\nOver the long horizon the divergent explicit methods leave the analytic reference entirely, whereas the bounded symplectic, implicit-midpoint and Gauss(2) methods retain a usable trajectory error for the full 10^4 time units at Float32/Float64. In the two half precisions the coarse step and the round-off floor together put the trajectory error at order one well before the end, even for the methods whose energy stays bounded — bounded energy does not imply a tracked orbit.","title":"Solution error"},{"category":"section","location":"harmonic_oscillator/#Phase-space-trajectory-2","page":"Harmonic Oscillator","text":"(Image: Phase-space trajectory, Euler methods)\n\n(Image: Phase-space trajectory, other methods)\n\n(Image: Phase-space trajectory, Gauss(2) variants)","title":"Phase-space trajectory"},{"category":"section","location":"#ReducedPrecision.jl","page":"Home","text":"Accuracy and long-time stability of geometric vs. non-geometric integrators in reduced floating-point precision.","title":"ReducedPrecision.jl"},{"category":"section","location":"#Overview","page":"Home","text":"ReducedPrecision studies how numerical integrators behave when run in BFloat16, Float16, Float32 and Float64, and in particular how geometric (symplectic) integrators compare to non-geometric ones with respect to\n\naccuracy — the error of the computed solution relative to a reference, and\nlong-time stability — whether the energy error stays bounded over long integration times.\n\nThe study uses six example problems from GeometricProblems.jl, each integrated with a range of symplectic and non-symplectic methods of GeometricIntegrators.jl, and each run in two scenarios (a fine short-horizon run and a coarser long-horizon run). The four Hamiltonian problems (harmonic oscillator, pendulum, double pendulum, Toda lattice) compare Euler-type methods, higher- order Runge–Kutta methods, and a group of partitioned Gauss(2) variants; the two degenerate-Lagrangian problems (Lotka–Volterra 2D and 4D) compare several flavours of variational implicit-midpoint integrator.\n\nThe implicit solves use the trust-region DogLeg nonlinear solver of SimpleSolvers.jl by default, which is more robust in reduced precision than a line-search Newton iteration.\n\nA central goal of the implementation is type purity: every library in the stack (GeometricIntegrators, GeometricIntegratorsBase, GeometricSolutions, GeometricEquations, GeometricBase, SimpleSolvers) must honour the requested precision and never silently promote to Float64. This is checked for every run (see Methodology).","title":"Overview"},{"category":"section","location":"#Key-findings-at-a-glance","page":"Home","text":"Symplectic integrators conserve energy. Symplectic Euler A/B and the implicit midpoint rule keep the energy error bounded (oscillating around a small value) over the entire integration, whereas explicit Euler grows without bound and implicit Euler dissipates. This is the expected qualitative difference between geometric and non-geometric integrators, and it holds at every precision.\nPrecision sets the error floor. For the energy-conserving methods the size of the (bounded) energy error is set by the working precision — for the harmonic oscillator at Δt = 0.1, the implicit midpoint rule settles at roughly 1e-2 (BFloat16), 9e-3 (Float16), 2e-6 (Float32) and 5e-15 (Float64).\nExponent range buys nothing; significand bits do. BFloat16 and Float16 are both 16-bit, but BFloat16 spends three of its significand bits on Float32's exponent range. On these bounded Hamiltonian problems that range is never needed, so BFloat16 is simply the coarser of the two — its error floor sits a factor of ≈ 8 above Float16's, exactly the ratio of their eps.\nReduced precision is type-pure. No implicit promotion to Float64 occurs in any library, for any method or problem, including the hand-built half-precision constructions of the double pendulum and Toda lattice.\nMost of what looks like a half-precision limit is a bookkeeping limit. Three quantities are easily carried or compared at the working precision without needing to be — the clock, the initial guess's interpolation node, and the solver's residual tolerance — and each then produces failures that read as hardware limits. A T-typed clock stops advancing once ulp(t) ≥ Δt (from t ≈ 128 in Float16, t ≈ 16 in BFloat16 at Δt = 0.1), which alone would cap the horizons studied and break the implicit solves. Stepping in a local time frame and taking the initial guess from the tableau's normalised nodes remove that dependence exactly for these autonomous problems, and the framework's residual tolerance is scaled to the precision and stage size. Across all eight Hamiltonian runs — every method at every precision — exactly one integration fails.\nWhat remains is genuinely precision-limited: the round-off floor on the achievable energy error, and the degenerate-Lagrangian Lotka–Volterra variational integrators, which break down in half precision — those are singular systems with a log(q) one-form, so the nonlinear iterate must stay in the positive orthant, and 8–11 significand bits are not enough to keep it there.\n\nSee Findings for the full discussion.","title":"Key findings at a glance"},{"category":"section","location":"#Running-the-experiments","page":"Home","text":"# from the package root, with the project activated\njulia --project=. scripts/harmonic_oscillator.jl\njulia --project=. scripts/pendulum.jl\njulia --project=. scripts/double_pendulum.jl\njulia --project=. scripts/toda_lattice.jl\njulia --project=. scripts/lotka_volterra_2d.jl\njulia --project=. scripts/lotka_volterra_4d.jl\n# coarser-step variants\njulia --project=. scripts/harmonic_oscillator_longtime.jl\njulia --project=. scripts/pendulum_longtime.jl\njulia --project=. scripts/double_pendulum_longtime.jl\njulia --project=. scripts/toda_lattice_longtime.jl\njulia --project=. scripts/lotka_volterra_2d_longtime.jl\njulia --project=. scripts/lotka_volterra_4d_longtime.jl\n\nAlternatively, regenerate every figure at once with the runner (it discovers scripts/*.jl automatically and runs them in a single Julia session, so the shared packages compile only once):\n\njulia --project=. scripts/run_all.jl\n\nEach script writes its figures to plots/. The documentation embeds those figures, so the scripts must be run before building the docs.","title":"Running the experiments"},{"category":"section","location":"double_pendulum/#Double-Pendulum","page":"Double Pendulum","text":"The double pendulum is a stiff, chaotic, two-degree-of-freedom system with dimensional parameters (g = 980665). It is generated symbolically by EulerLagrange and has no ::Type{T} constructor, so the initial conditions, timespan, timestep and parameters are built at precision T by hand. There is no closed-form solution; the reference is a Float64 Gauss(8) run. The trajectory plots use the configuration space (theta_1 theta_2).","title":"Double Pendulum"},{"category":"section","location":"double_pendulum/#Short-scenario-(Δt-0.01,-t-10)","page":"Double Pendulum","text":"","title":"Short scenario (Δt = 0.01, t ≤ 10)"},{"category":"section","location":"double_pendulum/#Energy-error","page":"Double Pendulum","text":"(Image: Energy error, Euler methods)\n\n(Image: Energy error, other methods)\n\n(Image: Energy error, Gauss(2) variants)\n\nAll twelve methods run at all four precisions, and the usual ordering holds — the implicit (Gauss) rules keep the energy error far below the Euler methods. The 2 × 2 group is particularly clean here at Float64: the two explicit rules both drift to order 1e-1, implicit midpoint stays around 1e-4, and implicit RK4 around 1e-10 — symplecticity setting the qualitative behaviour, the order setting the level. In the two half precisions all four collapse onto a common round-off floor (≈ 1e-1 at BFloat16, ≈ 1e-2 at Float16), so neither the order nor the symplecticity is visible: at 8–11 significand bits, arithmetic dominates the method entirely.\n\nThis is the scenario in which a global clock costs the most. On the problem's own T-typed grid the three implicit methods throw a NaN in the nonlinear-solver direction at Float16, and neither the trust-region DogLeg solver nor a MidpointExtrapolation initial guess reliably avoids it — because the obstruction is not the solver but the clock. Hermite extrapolation reads its interval off the stored history times, and Float16 resolves only about 0.004 near t = 5 — comparable to Delta t = 001 — so the differenced interval, and hence the initial guess, is materially wrong. Advancing the solution step in a local time frame makes the interval exact, and every method completes at every precision.\n\nThe four partitioned-Gauss(2) variants are therefore informative at every precision. The symplectic-vs-duplicated tableau choice and the rounding-compensation coefficients â b̂ ĉ produce visibly different energy-error fine structure at Float32/Float64; in half precision the round-off floor swamps those differences.","title":"Energy error"},{"category":"section","location":"double_pendulum/#Solution-error","page":"Double Pendulum","text":"(Image: Solution error, Euler methods)\n\n(Image: Solution error, other methods)\n\n(Image: Solution error, Gauss(2) variants)\n\nAgainst the Gauss(8) reference every method tracks closely until the chaotic divergence sets in, after which all trajectory errors saturate at order one. The lower the precision the earlier that happens — earliest at BFloat16, then Float16 — since a chaotic system amplifies the round-off floor exponentially. This is the mechanism by which reduced precision actually hurts here: not a failed solve, but a shorter predictability horizon.","title":"Solution error"},{"category":"section","location":"double_pendulum/#Configuration-space-trajectory","page":"Double Pendulum","text":"(Image: Configuration-space trajectory, Euler methods)\n\n(Image: Configuration-space trajectory, other methods)\n\n(Image: Configuration-space trajectory, Gauss(2) variants)\n\nThe methods track the reference until the chaotic divergence sets in, departing from it progressively earlier as the precision drops.","title":"Configuration-space trajectory"},{"category":"section","location":"double_pendulum/#Coarse-scenario-(Δt-0.1,-t-10)","page":"Double Pendulum","text":"","title":"Coarse scenario (Δt = 0.1, t ≤ 10)"},{"category":"section","location":"double_pendulum/#Energy-error-2","page":"Double Pendulum","text":"(Image: Energy error, Euler methods)\n\n(Image: Energy error, other methods)\n\n(Image: Energy error, Gauss(2) variants)\n\nAt the ten-times-coarser step Δt = 0.1 (over the same t ≤ 10 horizon as the fine run) every solve now stays stable — with the line-search Newton/Backtracking solver capped at 100 iterations, nothing trips the divergence guard. The geometric methods keep the smallest energy error: symplectic Euler A/B, the two Gauss rules and the partitioned Gauss(2) variants stay bounded around 1e-2–1e-1, while the non-geometric methods drift up toward order one (explicit Euler worst, then explicit midpoint and implicit Euler). Every method completes at every precision, which at this coarse step depends on the same clock-independence as the fine-step scenario — see Initial guess. Reduced precision merely raises the error floor; the qualitative ranking is the same at all four precisions. As always for this chaotic system the fine Δt = 0.01 run is the more informative one.","title":"Energy error"},{"category":"section","location":"double_pendulum/#Solution-error-2","page":"Double Pendulum","text":"(Image: Solution error, Euler methods)\n\n(Image: Solution error, other methods)\n\n(Image: Solution error, Gauss(2) variants)\n\nAt the coarse step the trajectory error against the Gauss(8) reference grows quickly for every method as the chaotic orbits separate, with the geometric and Gauss(2) methods retaining the smallest error before the divergence dominates.","title":"Solution error"},{"category":"section","location":"double_pendulum/#Configuration-space-trajectory-2","page":"Double Pendulum","text":"(Image: Configuration-space trajectory, Euler methods)\n\n(Image: Configuration-space trajectory, other methods)\n\n(Image: Configuration-space trajectory, Gauss(2) variants)","title":"Configuration-space trajectory"}]
}
